// PM2 конфигурация для back-chat.ap.kz
// WebSocket сервер вынесен в отдельную папку /var/www/user/data/www/websocket-chat/
module.exports = {
  apps: [
    // Здесь могут быть другие приложения Laravel если нужно
  ]
};
